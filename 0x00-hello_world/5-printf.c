#include <stdio.h>
/**
 * main - Entry point
 *
 * Return - Always return 0 (Success)
 *
 */
int main{
  char message = "\with proper grammar, but the outcome is a piece of art,";
  printf(message)
    return 0
}
